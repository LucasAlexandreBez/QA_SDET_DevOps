services:
  httpbin:
    image: kennethreitz/httpbin
    container_name: httpbin
    ports:
      - "9003:80"
    networks:
      - qa-network
    volumes:
      - httpbin_data:/app/

volumes:
  httpbin_data:
    name: qa-httpbin-data

networks:
  qa-network:
    external: true